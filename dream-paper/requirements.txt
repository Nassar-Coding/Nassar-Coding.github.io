# Broad development bounds ONLY. The AUTHORITATIVE environment for
# reproducing the published numbers is reproducibility/pip-freeze.txt
# (Python 3.11.15; pandas 3.0.3): install with
#   pip install -r reproducibility/pip-freeze.txt
# The bounds below are kept consistent with that environment's major
# versions so this file cannot silently install a different pandas major.
numpy>=2.4,<3
pandas>=3.0,<4
pyarrow>=14
scikit-learn>=1.9,<2
lightgbm>=4.6,<5
scipy>=1.17,<2
statsmodels>=0.14,<0.15
matplotlib>=3.10,<4
pyyaml>=6
pytest>=9
