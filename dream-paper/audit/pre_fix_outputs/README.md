# Pre-correction outputs (audit only)

These are the metric and table artifacts as they existed IMMEDIATELY BEFORE
the R1-F1 correction (repository state `4ba0068`, 2026-06-11), extracted
verbatim from git history. They are retained solely for audit of the
correction described in `../../CORRECTIONS.md` and are NOT used by the
paper; every published number derives from the corrected run and is bound
to it by the provenance-hash guard (G11).
